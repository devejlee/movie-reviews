import styled from '@emotion/styled'
import { baseStyle } from '../../baseStyles'

export const StyledWrap = styled.div`
  ${baseStyle}
  h2 {
    margin-bottom: 14px;
  }
  input {
    background: #f5f5f5;
  }
`